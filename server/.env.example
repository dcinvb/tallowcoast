# Port for the API server (default: 3001)
PORT=3001

# Allowed CORS origin for the frontend (default: http://localhost:5173)
CORS_ORIGIN=http://localhost:5173

# Optional: directory for data files (default: ./data)
# DATA_DIR=./data

# Optional: path to waitlist JSON file (default: DATA_DIR/waitlist.json)
# DATA_FILE=./data/waitlist.json
